language: elixir
sudo: false

cache:
  directories:
    - deps
    - _build/test

elixir: 1.8.1
otp_release: 21.3

matrix:
  include:
    - elixir: 1.7.0
      otp_release: 21.1

script: MIX_ENV=test mix coveralls.travis